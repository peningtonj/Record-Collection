*.iml
.db
.kotlin
.gradle
**/build/
.vscode/
xcuserdata
!src/**/build/
local.properties
.idea
.DS_Store
captures
.externalNativeBuild
.cxx
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings

# Local databases (pre-Firebase era + backups) — never commit
/composeApp/record_collection.db
*.db
*.db-shm
*.db-wal

# Runtime query logs — see docs/TECH_DEBT.md 2.3
/composeApp/logs/

# Firebase project config — provide your own; see README. TECH_DEBT 2.4
/composeApp/google-services.json
/composeApp/firebase.properties

# Python
__pycache__/
*.pyc
# Firebase Admin SDK service-account keys — NEVER commit (full project admin)
serviceAccountKey*.json
*-firebase-adminsdk-*.json
# local Python virtualenvs
records/
.venv/
venv/

# Standalone one-off SQLite->Firestore migration tool — kept on disk for reference,
# not part of the main build (settings.gradle.kts). See docs/MIGRATIONS.
/migration-reporter/

# Ad-hoc local backups
/backups/
